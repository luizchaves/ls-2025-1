import Math, { minus, multiply, divide } from './lib.js';
import { sqrt } from 'mathjs';

console.log(Math.sum(1, 1)); //=> 2
console.log(minus(1, 1)); //=> 0
console.log(multiply(2, 1)); //=> 2
console.log(divide(2, 1)); //=> 2
console.log(sqrt(4)); //=> 2
